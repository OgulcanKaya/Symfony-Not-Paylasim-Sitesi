{% extends 'adminbase.html.twig' %}

{% block title %}Notes{% endblock %}

{% block body %}

    <div class="dashboard-wrapper">
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">

                <h1> Show Notes List</h1>

                <table class="table">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ note.id }}</td>
                    </tr>
                    <tr>
                        <th>Title</th>
                        <td>{{ note.title }}</td>
                    </tr>
                    <tr>
                        <th>Keywords</th>
                        <td>{{ note.keywords }}</td>
                    </tr>
                    <tr>
                        <th>Description</th>
                        <td>{{ note.description }}</td>
                    </tr>
                    <tr>
                        <th> Üniversite </th>
                        <td> {{ note.university.name }}</td>
                    </tr>
                    <tr>
                        <th>Pdf</th>
                        <td><a href="{{ asset ('uploads/pdf/' ~note.pdf)}}"> Burdan indirebilirsiniz</a></td>
                    </tr>
                    <tr>
                        <th>Image</th>
                        <td><img src="{{ asset ('uploads/images/' ~note.image) }}" height="100"></td>
                    </tr>
                    <tr>
                        <th>Status</th>
                        <td>{{ note.status }}</td>
                    </tr>
                    <tr>
                        <th>Season_at</th>
                        <td>{{ note.seasonAt ? note.seasonAt|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Created_at</th>
                        <td>{{ note.createdAt ? note.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Updated_at</th>
                        <td>{{ note.updatedAt ? note.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    </tbody>
                </table>

                <a href="{{ path('admin_notes_index') }}"class="btn btn-rounded btn-primary">Back to List</a>

                <a href="{{ path('admin_notes_edit', {'id': note.id}) }}"class="btn btn-rounded btn-success">Edit</a>

                {{ include('admin/notes/_delete_form.html.twig') }}


            </div>
        </div>
    </div>

{% endblock %}