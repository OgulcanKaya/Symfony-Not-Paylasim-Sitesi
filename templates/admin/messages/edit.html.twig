{% extends 'adminbase.html.twig' %}

{% block title %}Edit Category Page{% endblock %}

{% block body %}
    <!-- wrapper  -->
    <!-- ============================================================== -->
    <div class="dashboard-wrapper">
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">

                {{ include('admin/messages/_form.html.twig', {'button_label': 'Update'}) }}
                <table>
                    <tr><a href="{{ path('admin_messages_index') }}"class="btn btn-rounded btn-primary">Back to List</a></tr>
                    <tr>   {{ include('admin/messages/_delete_form.html.twig') }}</tr>
                </table>
            </div>
        </div>
    </div>

{% endblock %}